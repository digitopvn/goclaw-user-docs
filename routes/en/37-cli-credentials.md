# CLI Credentials

**Route:** `/cli-credentials`
**Access:** Admin

## What It Shows
CLI authentication credentials management page for managing stored CLI auth tokens.

## Actions
- **Create credential** — add new CLI auth credential
- **Delete credential** — remove existing credential
- **List credentials** — view all stored CLI credentials

## Sub-features
- Credentials used for authenticating CLI tools against the gateway
