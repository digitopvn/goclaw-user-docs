---
title: "Monitoring and Logs"
description: "Monitor activity and view logs in GoClaw"
---

# Monitoring and Logs

## Overview

GoClaw provides 5 monitoring tools: Traces (LLM calls), Events (real-time), Activity (audit log), Logs (system log), and Usage Stats. All tools are grouped under the **Monitoring** sidebar section.

**Routes:**
- `/traces` — Operator+
- `/traces/:id` — Operator+
- `/events` — Operator+
- `/activity` — Operator+
- `/logs` — Admin

---

## Guide

### View Traces

Traces record the full activity of an agent per request: LLM calls, tool calls, token count, latency, cost.

1. Go to **Traces** in the main menu
2. Filter by: Agent, Channel, Status (`running` / `success` / `error` / `cancelled`)
3. Click a trace to view details

**Trace information:**

| Field | Description |
|-------|-------------|
| Duration | Total execution time |
| Input / Output tokens | Total LLM tokens used |
| Cost | Estimated cost |
| Status | Final status |
| Tool calls | Number of tools invoked |

**Stop a running trace:** Click the stop button on the processing row.

**Export trace:** Click **Export** in the trace detail → downloads `trace-{id}.json.gz`.

### Trace Detail — Span Tree

Each trace contains multiple spans in a hierarchy:

```
Agent Span (root)
  LLM Call Span 1 — model, tokens, finish reason
  Tool Span: exec
  LLM Call Span 2
  Tool Span: read_file
  LLM Call Span N
```

**Span types:**

| Type | Description |
|------|-------------|
| `agent` | Root span, encompasses the entire agent run |
| `llm_call` | A single LLM provider call |
| `tool_call` | A single tool execution |
| `embedding` | Create vector embedding |
| `event` | Discrete event (no duration) |

**Prompt Cache Metrics** (Anthropic Claude): displays `cache_read_tokens` and `cache_create_tokens` in `llm_call` spans.

**Verbose Mode** (view full content): set env var `GOCLAW_TRACE_VERBOSE=1`. Stores up to 200KB per input/output span.

### Events — Real-Time Events

**Route `/events`:** Live event stream via WebSocket in chronological order.

- **Filter by category:** team.task / team.message / agent / team.crud / agent_link
- **Filter by group / user / chat** — dropdown
- **Pause / Resume** stream
- **Clear all events** — clear the current view

Events auto-scroll to the bottom when new events arrive.

### Activity — Admin Audit Log

**Route `/activity`:** Paginated audit log table — action, actor, entity type, entity ID, IP address, timestamp.

- Filter by action type (e.g. `agent.created` / `agent.updated` / `agent.deleted`)
- Filter by entity type
- View only, no data modification actions

**Admin actions recorded:**
- Create / revoke API key
- Change agent configuration
- Approve / deny browser pairing
- Change RBAC and access permissions
- Create / delete cron job

### Logs — System Log

**Route `/logs`:** Real-time log viewer, dark terminal interface.

Each entry: timestamp, level (debug/info/warn/error), source, message, attributes.

**Actions:**
- Select log level before starting monitoring
- **Start monitoring** — stream logs live
- **Stop monitoring** — pause stream
- **Filter by level** — pills: debug / info / warn / error
- **Text search** — filter by keyword
- **Clear log** — clear current view

**Filter security events** from log file:
```bash
grep "security." goclaw.log
```

| Event | Meaning |
|-------|---------|
| `security.injection_detected` | Prompt injection detected |
| `security.injection_blocked` | Message blocked |
| `security.rate_limited` | Request rate limited |
| `security.cors_rejected` | WebSocket connection rejected |
| `security.message_truncated` | Message truncated due to excess length |

---

## UI

### Traces Page (`/traces`)

**Displays:** Paginated table: name, status, duration, input/output + cache tokens, span count, timestamp. Agent and channel filters.

**Trace Detail Dialog:**
- Header: Copy Trace ID | Export | Stop (running trace)
- Summary grid: Name, Status, Duration, Channel, Tokens, Span count, Start time, Parent trace (clickable)
- Preview blocks: input + copy | output + copy | error block (red border)
- Span tree: type badge, name, tokens, cache/thinking, duration, status. Click span → detail panel: timestamp, model, token breakdown, reasoning metadata, input/output preview

### Trace Detail Page (`/traces/:id`)

Opens as dialog on the `/traces` page. Collapsible span tree. Time and token breakdown per span. **Navigate** to previous/next trace.

### Events Page (`/events`)

Real-time event stream. Auto-scrolls to bottom. WebSocket real-time subscription.

### Activity Page (`/activity`)

Paginated audit log table. View only.

### Logs Page (`/logs`)

Dark terminal interface. Auto-scrolls when new logs arrive. Select log level before starting monitoring.

---

## Usage Stats

The system aggregates stats hourly into the `usage_snapshots` table.

| Metric | Description |
|--------|-------------|
| `request_count` | Number of agent runs |
| `error_count` | Number of failed runs |
| `unique_users` | Number of unique users |
| `input_tokens` / `output_tokens` | Total tokens |
| `total_cost` | Total estimated cost |
| `tool_call_count` | Total tool calls |
| `avg_duration_ms` | Average duration per run |
| `cache_read_tokens` | Tokens read from prompt cache |

**Aggregation cycle:** `SnapshotWorker` runs every hour at minute 5 (HH:05:00 UTC). On startup, automatically backfills missed hours.

**Cost formula:**
```
Cost = (input_tokens x InputCostPerM) / 1,000,000
     + (output_tokens x OutputCostPerM) / 1,000,000
     + (cache_read_tokens x CacheReadCostPerM) / 1,000,000
     + (cache_create_tokens x CacheCreateCostPerM) / 1,000,000
```

---

## OpenTelemetry — Export to External Systems

Supports exporting spans to Jaeger, Grafana Tempo, Datadog via OTLP.

Add to `config.json`:
```json
{
  "tracing": {
    "otel": {
      "endpoint": "localhost:4317",
      "protocol": "grpc",
      "insecure": true,
      "service_name": "goclaw-gateway",
      "headers": {}
    }
  }
}
```

| Parameter | Description |
|-----------|-------------|
| `endpoint` | OTLP endpoint (gRPC: 4317, HTTP: 4318) |
| `protocol` | `grpc` (default) or `http` |
| `insecure` | Skip TLS for dev environments |
| `service_name` | Default: `goclaw-gateway` |

When OTel is configured, spans are written to both PostgreSQL and the OTLP backend. Batch: up to 100 spans every 5 seconds.

> Note: The OTel package adds ~15–20 MB to the binary size. No performance impact when OTel is not configured.

---

## Notes

- Route `/logs` requires Admin; other routes require Operator+
- Verbose mode (`GOCLAW_TRACE_VERBOSE=1`) can significantly increase storage usage
- System logs are written to stdout of the GoClaw process

---

## See Also

- [security-and-permissions.md](security-and-permissions.md) — Security events and injection detection
- [cron-and-scheduling.md](cron-and-scheduling.md) — Cron job run log
